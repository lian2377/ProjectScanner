This is a project for project programs tracking.
Developed in Python3.6

----------USAGE----------
Scanner.py
    usage: Scanner.py [-h] [-v | -q] [-s] projectpath [projectpath ...]

    Get include relationes in the given directories.

    positional arguments:
      projectpath      project root path

    optional arguments:
      -h, --help       show this help message and exit
      -v, --verbose    show verbose output
      -q, --quiet      no output message
      -s, --sensitive  case sensitive file name
        
GetSuffixes.py
    usage: GetSuffixes.py [-h] [-q] [-s] projectpath [projectpath ...]

    Get include relationes in the given directories.

    positional arguments:
      projectpath      project root path

    optional arguments:
      -h, --help       show this help message and exit
      -q, --quiet      no output message
      -s, --sensitive  case sensitive file name